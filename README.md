# Project Repository

This is the initial README file for the project.

Frontend quick start with mock data:
- See events_frontend/.env.example. Copy to events_frontend/.env and set REACT_APP_USE_MOCK=true to run without a backend.
- Start frontend: cd events_frontend && npm start